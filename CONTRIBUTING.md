# Contributing to HQ Studio

## 📝 Conventional Commits

Мы используем [Conventional Commits](https://www.conventionalcommits.org/) для стандартизации сообщений коммитов.

### Формат коммита

```
<type>(<scope>): <description>

[optional body]

[optional footer(s)]
```

### Типы коммитов

| Тип | Описание | Версия |
|-----|----------|--------|
| `feat` | Новая функциональность | minor |
| `fix` | Исправление бага | patch |
| `docs` | Документация | - |
| `style` | Форматирование (не влияет на код) | - |
| `refactor` | Рефакторинг кода | patch |
| `perf` | Улучшение производительности | patch |
| `test` | Добавление/изменение тестов | - |
| `build` | Сборка/зависимости | - |
| `ci` | CI/CD конфигурация | - |
| `chore` | Прочие изменения | - |
| `revert` | Откат изменений | - |

### Области (scope)

| Scope | Описание |
|-------|----------|
| `api` | HQStudio.API |
| `web` | HQStudio.Web |
| `desktop` | HQStudio.Desktop |
| `tests` | Тесты |
| `docker` | Docker конфигурация |
| `ci` | CI/CD |
| `deps` | Зависимости |

### Примеры

```bash
# Новая функция
feat(api): добавлен endpoint для экспорта заказов

# Исправление бага
fix(web): исправлена ошибка валидации формы

# Документация
docs: обновлён README

# Рефакторинг
refactor(desktop): оптимизирован DataService

# Breaking change
feat(api)!: изменён формат ответа API

BREAKING CHANGE: поле `status` теперь возвращает enum вместо строки
```

## 🚀 Как делать коммиты

### Вариант 1: Интерактивный режим (рекомендуется)

```bash
npm run commit
```

### Вариант 2: Вручную

```bash
git commit -m "feat(api): добавлена авторизация"
```

## 🔄 Автоматический релиз

При пуше в `main` автоматически:
1. Запускаются тесты
2. Анализируются коммиты
3. Определяется новая версия (semver)
4. Генерируется CHANGELOG.md
5. Создаётся GitHub Release
6. Собираются Docker images
7. Собирается Desktop приложение

## 🧪 Тестирование

```bash
# API тесты
dotnet test HQStudio.API.Tests

# Web тесты
cd HQStudio.Web && npm test

# Desktop тесты
dotnet test HQStudio.Desktop.Tests
```

## 📋 Pull Request

1. Создайте ветку от `main`
2. Сделайте изменения
3. Убедитесь что тесты проходят
4. Создайте PR с описанием изменений
