<UserControl x:Class="HQStudio.Views.ActivityLogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,24">
            <StackPanel>
                <TextBlock Text="📋 ЖУРНАЛ ОТВЕТСТВЕННОСТИ" Style="{DynamicResource TextLabel}" Margin="0,0,0,8"/>
                <TextBlock Text="История всех действий пользователей в системе" 
                           Style="{DynamicResource TextSmall}"/>
            </StackPanel>
            <Button Content="🔄 Обновить" Command="{Binding RefreshCommand}" 
                    Style="{DynamicResource ButtonPrimary}" HorizontalAlignment="Right" VerticalAlignment="Top"/>
        </Grid>

        <!-- Stats Cards -->
        <Grid Grid.Row="1" Margin="0,0,0,24" Visibility="{Binding IsApiConnected, Converter={StaticResource BoolToVisibility}}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Style="{DynamicResource CardHoverable}" Margin="0,0,12,0">
                <StackPanel>
                    <Border Width="48" Height="48" CornerRadius="12" Background="{DynamicResource BgIconBrush}" 
                            HorizontalAlignment="Left" Margin="0,0,0,16">
                        <TextBlock Text="📅" FontSize="22" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <TextBlock Text="{Binding Stats.TotalToday, FallbackValue=0}" FontSize="36" FontWeight="Bold" 
                               Foreground="{DynamicResource TextPrimaryBrush}"/>
                    <TextBlock Text="Сегодня" Style="{DynamicResource TextSmall}" Margin="0,4,0,0"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="1" Style="{DynamicResource CardHoverable}" Margin="6,0">
                <StackPanel>
                    <Border Width="48" Height="48" CornerRadius="12" Background="{DynamicResource BgIconBrush}" 
                            HorizontalAlignment="Left" Margin="0,0,0,16">
                        <TextBlock Text="📆" FontSize="22" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <TextBlock Text="{Binding Stats.TotalWeek, FallbackValue=0}" FontSize="36" FontWeight="Bold" 
                               Foreground="{DynamicResource TextPrimaryBrush}"/>
                    <TextBlock Text="За неделю" Style="{DynamicResource TextSmall}" Margin="0,4,0,0"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="2" Style="{DynamicResource CardHoverable}" Margin="12,0,0,0">
                <StackPanel>
                    <Border Width="48" Height="48" CornerRadius="12" Background="{DynamicResource BgIconBrush}" 
                            HorizontalAlignment="Left" Margin="0,0,0,16">
                        <TextBlock Text="📊" FontSize="22" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <TextBlock Text="{Binding Stats.TotalAll, FallbackValue=0}" FontSize="36" FontWeight="Bold" 
                               Foreground="{DynamicResource TextPrimaryBrush}"/>
                    <TextBlock Text="Всего записей" Style="{DynamicResource TextSmall}" Margin="0,4,0,0"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Not Connected Warning -->
        <Border Grid.Row="2" Style="{DynamicResource Card}" Padding="40" 
                Visibility="{Binding IsApiConnected, Converter={StaticResource InverseBoolToVisibilityConverter}, FallbackValue=Collapsed}">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock Text="⚠️" FontSize="48" HorizontalAlignment="Center" Margin="0,0,0,16"/>
                <TextBlock Text="Нет подключения к API" FontSize="18" FontWeight="SemiBold" 
                           Foreground="{DynamicResource TextPrimaryBrush}" HorizontalAlignment="Center"/>
                <TextBlock Text="Журнал ответственности доступен только при подключении к серверу" 
                           Style="{DynamicResource TextSmall}" HorizontalAlignment="Center" Margin="0,8,0,0"/>
            </StackPanel>
        </Border>

        <!-- Main Content -->
        <Border Grid.Row="2" Style="{DynamicResource Card}" 
                Visibility="{Binding IsApiConnected, Converter={StaticResource BoolToVisibility}}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Filters -->
                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,16">
                    <TextBlock Text="Источник:" VerticalAlignment="Center" Margin="0,0,10,0" 
                               Foreground="{DynamicResource TextSecondaryBrush}"/>
                    <ComboBox Width="120" ItemsSource="{Binding Sources}" SelectedItem="{Binding SelectedSource}" 
                              Style="{DynamicResource ComboBoxStyle}" Margin="0,0,20,0"/>

                    <TextBlock Text="Пользователь:" VerticalAlignment="Center" Margin="0,0,10,0" 
                               Foreground="{DynamicResource TextSecondaryBrush}"/>
                    <ComboBox Width="180" ItemsSource="{Binding Users}" SelectedItem="{Binding SelectedUser}" 
                              DisplayMemberPath="UserName" Style="{DynamicResource ComboBoxStyle}" Margin="0,0,20,0"/>

                    <Button Content="Применить" Command="{Binding ApplyFiltersCommand}" 
                            Style="{DynamicResource ButtonSecondary}"/>
                </StackPanel>

                <!-- Activity Log List -->
                <DataGrid Grid.Row="1" ItemsSource="{Binding ActivityLogs}" 
                          AutoGenerateColumns="False" IsReadOnly="True"
                          GridLinesVisibility="Horizontal" HeadersVisibility="Column"
                          Background="Transparent" BorderThickness="0"
                          RowBackground="Transparent" AlternatingRowBackground="#0AFFFFFF"
                          Style="{DynamicResource DataGridStyle}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Дата" Binding="{Binding FormattedDate}" Width="130"/>
                        <DataGridTextColumn Header="Пользователь" Binding="{Binding UserName}" Width="140"/>
                        <DataGridTextColumn Header="Действие" Binding="{Binding Action}" Width="*"/>
                        <DataGridTextColumn Header="Тип" Binding="{Binding EntityType}" Width="100"/>
                        <DataGridTextColumn Header="ID" Binding="{Binding EntityId}" Width="60"/>
                        <DataGridTemplateColumn Header="Источник" Width="100">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding SourceIcon}" Margin="0,0,5,0"/>
                                        <TextBlock Text="{Binding Source}"/>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTextColumn Header="IP" Binding="{Binding IpAddress}" Width="120"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>

        <!-- Pagination -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0"
                    Visibility="{Binding IsApiConnected, Converter={StaticResource BoolToVisibility}}">
            <Button Content="◀ Назад" Command="{Binding PreviousPageCommand}" 
                    Style="{DynamicResource ButtonSecondary}" Margin="0,0,10,0"/>
            
            <TextBlock Text="{Binding PageInfo}" VerticalAlignment="Center" 
                       Margin="10,0,10,0" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryBrush}"/>
            
            <Button Content="Вперёд ▶" Command="{Binding NextPageCommand}" 
                    Style="{DynamicResource ButtonSecondary}" Margin="10,0,0,0"/>
        </StackPanel>
    </Grid>
</UserControl>
