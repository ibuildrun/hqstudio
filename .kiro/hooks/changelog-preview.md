---
event: onManual
description: "Предпросмотр релиза"
---

# CHANGELOG Preview

При ручном запуске этого хука:

1. Получи список коммитов с последнего релиза:
   ```
   git log --oneline $(git describe --tags --abbrev=0)..HEAD
   ```

2. Проанализируй типы коммитов по Conventional Commits:
   - feat → minor version bump
   - fix, perf, refactor → patch version bump
   - docs, style, test, ci, chore → no release

3. Покажи предпросмотр:
   ```
   📦 Предпросмотр следующего релиза
   
   Текущая версия: X.Y.Z
   Следующая версия: X.Y.Z (на основе коммитов)
   
   🚀 Новые возможности (feat):
   - ...
   
   🐛 Исправления (fix):
   - ...
   
   ⚡ Производительность (perf):
   - ...
   ```

4. Если нет коммитов для релиза, сообщи об этом
